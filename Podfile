# Uncomment the next line to define a global platform for your project
# platform :ios, '17.2'

target 'RMCharactersRev1' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftLint'

  # Pods for RMCharactersRev1

  target 'RMCharactersRev1Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RMCharactersRev1UITests' do
    # Pods for testing
  end

end
